import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Vuka Digital Group | Digital Marketing & Creative Solutions',
  description: 'Vuka Digital Group builds powerful brands and digital systems that drive real business growth. Strategic marketing, compelling creative content, and powerful digital technology.',
  generator: 'Vuka Digital Group',
  icons: {
    icon: '/images/vuka-logo.png',
    apple: '/images/vuka-logo.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
